- provide(:title, "Editing #{current_user.name}")

.container
  .row
    .col-sm-6
      .panel.panel-info
        .panel-heading Editing #{current_user.name}
        .panel-body
          = render 'form'

= link_to 'Back', '/dashboard'

-# %h1 Editing user
-# = render 'form'
-# = link_to 'Show', @user
-# |
-# \#{link_to 'Back', users_path}
